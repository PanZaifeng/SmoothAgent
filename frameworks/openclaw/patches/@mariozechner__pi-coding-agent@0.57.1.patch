diff --git a/dist/core/agent-session.d.ts b/dist/core/agent-session.d.ts
index 05cde97e490724b6381870031fe5ae93fc1e3a11..d28c9bdeebec69a043bd8f9989b242e1334412d3 100644
--- a/dist/core/agent-session.d.ts
+++ b/dist/core/agent-session.d.ts
@@ -417,6 +417,27 @@ export declare class AgentSession {
      * Saves to settings.
      */
     setFollowUpMode(mode: "all" | "one-at-a-time"): void;
+    /**
+     * Prepare compaction without writing to the session (read-only).
+     * Returns preparation data for lookahead or external compaction.
+     * @param _customInstructions Optional; reserved for future use
+     */
+    prepareCompact(_customInstructions?: string): Promise<{
+        messagesToSummarize: AgentMessage[];
+        turnPrefixMessages: AgentMessage[];
+        previousSummary?: string;
+        firstKeptEntryId?: string;
+        tokensBefore?: number;
+        fileOps: {
+            read: string[];
+            edited: string[];
+            written: string[];
+        };
+        settings: {
+            reserveTokens: number;
+        };
+        isSplitTurn?: boolean;
+    }>;
     /**
      * Manually compact the session context.
      * Aborts current agent operation first.
diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index 90622c2ec087ae9407a5016f7a173e8be8656abd..4ff8ef9307f241be06681281e1943efd26d09fca 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -1206,6 +1206,37 @@ export class AgentSession {
     // =========================================================================
     // Compaction
     // =========================================================================
+    /**
+     * Prepare compaction without writing to the session (read-only).
+     * Returns preparation data for use by lookahead or external compaction.
+     * @param _customInstructions Optional; reserved for future use (e.g. summary focus)
+     */
+    async prepareCompact(_customInstructions) {
+        const pathEntries = this.sessionManager.getBranch();
+        const settings = this.settingsManager.getCompactionSettings();
+        const preparation = prepareCompaction(pathEntries, settings);
+        if (!preparation) {
+            const lastEntry = pathEntries[pathEntries.length - 1];
+            if (lastEntry?.type === "compaction") {
+                throw new Error("Already compacted");
+            }
+            throw new Error("Nothing to compact (session too small)");
+        }
+        return {
+            messagesToSummarize: preparation.messagesToSummarize,
+            turnPrefixMessages: preparation.turnPrefixMessages,
+            previousSummary: preparation.previousSummary,
+            firstKeptEntryId: preparation.firstKeptEntryId,
+            tokensBefore: preparation.tokensBefore,
+            fileOps: {
+                read: Array.from(preparation.fileOps.read),
+                edited: Array.from(preparation.fileOps.edited),
+                written: Array.from(preparation.fileOps.written),
+            },
+            settings: { reserveTokens: preparation.settings.reserveTokens },
+            isSplitTurn: preparation.isSplitTurn,
+        };
+    }
     /**
      * Manually compact the session context.
      * Aborts current agent operation first.
diff --git a/dist/core/compaction/compaction.d.ts b/dist/core/compaction/compaction.d.ts
--- a/dist/core/compaction/compaction.d.ts
+++ b/dist/core/compaction/compaction.d.ts
@@ -5,7 +5,7 @@
  * and after compaction the session is reloaded.
  */
 import type { AgentMessage } from "@mariozechner/pi-agent-core";
-import type { Model, Usage } from "@mariozechner/pi-ai";
+import type { Model, SimpleStreamOptions, Usage } from "@mariozechner/pi-ai";
 import type { SessionEntry } from "../session-manager.js";
 import { type FileOperations } from "./utils.js";
 /** Details stored in CompactionEntry.details for file tracking */
@@ -91,7 +91,7 @@ export declare function findCutPoint(entries: SessionEntry[], startIndex: number
  * Generate a summary of the conversation using the LLM.
  * If previousSummary is provided, uses the update prompt to merge.
  */
-export declare function generateSummary(currentMessages: AgentMessage[], model: Model<any>, reserveTokens: number, apiKey: string, signal?: AbortSignal, customInstructions?: string, previousSummary?: string): Promise<string>;
+export declare function generateSummary(currentMessages: AgentMessage[], model: Model<any>, reserveTokens: number, apiKey: string, signal?: AbortSignal, customInstructions?: string, previousSummary?: string, extraOptions?: Pick<SimpleStreamOptions, "onPayload">): Promise<string>;
 export interface CompactionPreparation {
     /** UUID of first entry to keep */
     firstKeptEntryId: string;
diff --git a/dist/core/compaction/compaction.js b/dist/core/compaction/compaction.js
--- a/dist/core/compaction/compaction.js
+++ b/dist/core/compaction/compaction.js
@@ -420,8 +420,8 @@ export function findCutPoint(entries, startIndex, endIndex, keepRecentTokens) {
 /**
  * Generate a summary of the conversation using the LLM.
  * If previousSummary is provided, uses the update prompt to merge.
  */
-export async function generateSummary(currentMessages, model, reserveTokens, apiKey, signal, customInstructions, previousSummary) {
+export async function generateSummary(currentMessages, model, reserveTokens, apiKey, signal, customInstructions, previousSummary, extraOptions) {
     const maxTokens = Math.floor(0.8 * reserveTokens);
     // Use update prompt if we have a previous summary, otherwise initial prompt
     let basePrompt = previousSummary ? UPDATE_SUMMARIZATION_PROMPT : SUMMARIZATION_PROMPT;
@@ -444,8 +444,8 @@ export async function generateSummary(currentMessages, model, reserveTokens, api
         },
     ];
     const completionOptions = model.reasoning
-        ? { maxTokens, signal, apiKey, reasoning: "high" }
-        : { maxTokens, signal, apiKey };
+        ? { maxTokens, signal, apiKey, reasoning: "high", ...extraOptions }
+        : { maxTokens, signal, apiKey, ...extraOptions };
     const response = await completeSimple(model, { systemPrompt: SUMMARIZATION_SYSTEM_PROMPT, messages: summarizationMessages }, completionOptions);
     if (response.stopReason === "error") {
         throw new Error(`Summarization failed: ${response.errorMessage || "Unknown error"}`);
